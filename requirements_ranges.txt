# floclib dependency ranges — mirror of pyproject.toml [project.dependencies].
# Single source of truth is pyproject.toml; keep this file in sync.
#
# Core (tabular pipeline: feature table -> Beta -> Ka/Kb -> THRT)
numpy>=1.24,<2.0
pandas>=2.1,<2.2
scipy>=1.10,<2.0
scikit-learn>=1.1,<=1.5
pyswarms>=1.3,<1.4
matplotlib>=3.7,<=3.8
fastparquet>=2023,<=2024.11

# Optional [seg] extra (image segmentation stage):
#   scikit-image>=0.23,<0.24
#   opencv-python-headless>=4.6,<5.0
#   imageio>=2.31
# Optional [yaml] extra (pipeline config serialization):
#   pyyaml>=6.0
#
# Install everything:  pip install -e ".[seg,yaml]"
